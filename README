# Initial Setup.

To setup python you should:
1. Go to Edit > Project settings > Player > Other Settings > Configuration; Change there Scripting Runtime Version to '.NET 4.x Equivalent'
2. Copy from Assets/Plugins directory all the *.dll files;
3. Copy Assets/Python/Lib directory to your assets - it contains base python libraries.


Congratulations! Now you can use a python version 2.7 in you unity environment;
